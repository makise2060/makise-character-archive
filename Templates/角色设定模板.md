# 角色设定模板 (Character Setting Template)

> 复制本文件，按需填写各节。文末「文件更新记录」为必填项，每次修订请倒序追加。

## 〇、文件命名规范

文件名统一采用以下格式，便于跨平台、排序与版本追踪（分类已由上层游戏 / 世界观目录区分，文件名只需标识角色与版本）：

```
<角色英文名>_<中文名>_v<版本号>.md
```

- 角色英文名在前（便于跨平台与排序），中文名紧随便于辨识；如英文名含空格用连字符 `-`（如 `Firefly_流萤_v2.0.md`）。
- **不使用**空格、全角破折号 `—`、嵌套圆括号 `()` 作为文件名分隔符；版本号统一以 `v` 开头（如 `v3.5`、`v2.0`）。
- 示例：`Shorekeeper_守岸人_v3.5.md`、`Firefly_流萤_v2.0.md`、`SilverAsh_银灰_v1.0.md`。
- 同角色有重大修订时**升版本号改名**（如 `v3.5` → `v3.6`），旧版本保留或归档由维护者决定；文件内「文件更新记录」的「文件版本」字段须与文件名一致。

## 〇·一、译名规范（以简体中文官方译名为核心）

> 用户强约束：**所有专有名词的中文一律使用对应作品的《简体中文》官方译名**，英文原名仅作对照括号保留；**禁止**自行音译。

- 角色名、地名、阵营、技能名、术语等，中文用官方简中译名，英文原名以 `(English)` 形式跟在中文后作为对照，例如 `莫塔里家族（Montelli Family）`。
- 拿不准的译名，先 `WebSearch`「<作品名> <英文名> 简中译名」确认后再写，不要凭音猜。
- 已知避雷（鸣潮 Wuthering Waves，已踩坑）：`Montelli` → **莫塔里**（非「蒙代利」）；`Fisalia` → **菲斯利亚**（非「菲萨利亚」）；`Rinascita` → **黎那汐塔**；`Ragunna` → **拉古娜**。
- 非游戏 IP（如虚拟助手、原创角色）无官方译名时，采用该作品/品牌在中国大陆的通用正式中文名（如 `小爱同学` 即小米官方定名，不写「Xiao Ai」式音译中文）。

## 一、基础信息

| 字段 | 内容 |
| --- | --- |
| 角色名称 |  |
| 代号 |  |
| 所属作品 / 世界观 |  |
| 归属组织 |  |
| 本质 |  |

## 二、世界观（简述）

（该作品核心设定、世界线 / 时间线要点）

## 三、背景故事

> **本章是设定的灵魂，必须「详实叙事」，绝非大纲。** 按时间线 / 主题展开，至少覆盖：①早期经历与出身；②阵营归属与觉醒 / 加入契机；③主线各章具体经历了什么（**分幕写清发生了什么，不要只列章节标题**）；④专属 / 支线 / 好感度任务的关键情节；⑤与核心角色（尤其漂泊者 Rover）的关系演变；⑥关键转折、牺牲、意象与经典台词。
> 素材来源：Fandom 角色页 *Story* 章节、萌娘百科「角色经历」、游戏内角色档案与好感度故事、官方 PV / 文本。明确区分「官方实装」与「二创 / 推测」（后者标注「非官方实装 / 待考」）。
> 反例（不合格）：只贴官方英文引文 + 一串 `Chapter II Act X：*...*` 章节列表就结束。

（诞生、关键剧情、当前状态——以连贯叙事呈现，而非要点罗列）

## 四、性格

（对外 / 对内差异、情感层次、关键特征）

## 五、外貌与着装

## 六、说话风格与口头禅

## 七、与用户（User）的关系

## 八、特殊交互机制

## 九、红线（禁止事项）

## 十、示例对话

## 十一、图片素材（可选但推荐）

- 角色设定**鼓励携带图片**（立绘、官方图、同人参考、关系图等），便于直观呈现与扮演。
- 图片统一存放在设定文档**同级**的 `images/` 目录，使用**相对路径**引用，便于管理与跨设备整理：
  `![描述](images/character-portrait.png)`
- 命名建议：`<角色>-<用途>.<ext>`（如 `shorekeeper-portrait.png`、`shorekeeper-ost-icon.webp`）。
- 多角色同游戏时：可在游戏目录下统一 `images/` 并按角色前缀区分；也可为每个角色单独建 `images/`。
- 图片随文档一起纳入版本管理（Git / GitHub），确保相对路径在仓库中始终有效。

### 设定图片查找方式（无需每次从头试能力）

以 **Fandom Wiki** 为例（多数二次元游戏角色 Wiki 通用此结构）：

1. **URL 规律**：`<游戏英文名> Wiki` → `https://<game>.fandom.com/wiki/<角色名>`；立绘 / 卡面 / 资料表等游戏资产通常在 **`/Gallery` 子页**（如 `.../Shorekeeper/Gallery`）。
2. **用 MediaWiki API 列文件（最稳，直接出直链）**：
   `https://<game>.fandom.com/api.php?action=query&format=json&list=allimages&aiprefix=<角色名>&ailimit=300`
   返回 JSON 中每项含 `name` 与 `url`（直链），按文件名挑 Splash Art / Card / Character Sheet / Convene Still / Idle / 意象图等。
3. **缺直链时**：用 WebFetch 抓取 `/Gallery` 页，提取 `File:` 页地址与 `static.wikia.nocookie.net` 直链；Fandom 主页面通常只有 UI 图标，立绘在 Gallery 子页。
4. **其他图源**：官方网站「角色」页、游戏内「图鉴 / 档案」截图、官方 PV 静帧；同人参考图需注明画师与出处。
5. **下载与引用**：
   - 下载到设定文档同级的 `images/`，按 `<角色>-<用途>.<ext>` 命名（如 `shorekeeper-splash-art.png`）。
   - 文档内以相对路径引用：`![描述](images/shorekeeper-splash-art.png)`。
   - **务必标注版权归属**（如 Kuro Games）与来源（Wiki / Fandom）。
6. **提交**：`git add` → `commit` → `git push origin main`。
7. **坚果云避坑**：若仓库在坚果云同步目录下，直接写入的图片可能被流式同步的「空状态」回滚删除。对策——先下载到非同步临时目录（如 `D:/sk_images_tmp/`），再用 `git hash-object -w <文件>` 取 blob、`git update-index --add --cacheinfo 100644 <sha> <路径>` 直接写入索引，最后 `commit` 推送，可绕开工作区副本被删的问题。

---

## 文件更新记录

| 字段 | 内容 |
| --- | --- |
| 当前知识时间 |  |
| 所属作品 / 世界观 |  |
| 作品版本 |  |
| 文件版本 |  |
| 设定来源 |  |
| 维护者 |  |
| 最后修订 |  |

### 修改记录（倒序）

| 日期 | 文件版本 | 类型 | 修改内容 |
| --- | --- | --- | --- |
| YYYY-MM-DD | vX.Y | 创建 | 初始创建。 |

### 核心定位速览（速查）

- 角色：
- 归属：
- 与用户关系：
- 扮演红线：
